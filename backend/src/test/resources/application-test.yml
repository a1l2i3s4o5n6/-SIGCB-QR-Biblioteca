spring:
  datasource:
    url: ${TEST_DATASOURCE_URL:jdbc:postgresql://localhost:5432/sigcbqr_test}
    username: ${TEST_DATASOURCE_USERNAME:postgres}
    password: ${TEST_DATASOURCE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver